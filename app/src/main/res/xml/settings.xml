<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory
        android:title="네트워크 설정"
        android:key="pref_key_storage_settings">

        <ListPreference
            android:key="serverUrl"
            android:defaultValue="http://210.127.55.205:82"
            android:entries="@array/serverUrls"
            android:entryValues="@array/serverUrls_values"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="네트워크 설정"
            android:summary=""
            />

    </PreferenceCategory>
    <!--
    <CheckBoxPreference
        android:defaultValue="false"
        android:key="useUserName"
        android:summaryOff="사용자의 이름을 사용하지 않습니다"
        android:summaryOn="사용자의 이름을 사용합니다"
        android:title="사용자 이름 사용" />

    <EditTextPreference
        android:defaultValue="Mir"
        android:dependency="useUserName"
        android:key="userName"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="사용자 이름 설정" />
        -->





</PreferenceScreen>